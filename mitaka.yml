---
- hosts: mitaka-14
  vars:
    - controller_host: mitaka-14
  roles:
    - controller/common
    - controller/glance
  environment:
    OS_URL: "{{keystone_admin_url}}"
    OS_IDENTITY_API_VERSION: 3
    OS_TOKEN: "{{keystone_admin_token}}"
