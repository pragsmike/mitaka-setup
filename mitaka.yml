---
- hosts: mitaka
  vars:
    - controller_host: mitaka
  roles:
    - controller/common
  environment:
    OS_URL: "{{keystone_admin_url}}"
    OS_IDENTITY_API_VERSION: 3
    OS_TOKEN: "{{keystone_admin_token}}"
